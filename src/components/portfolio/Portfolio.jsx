import Section from "../layout/Section";

const Portfolio = () => {
    return (
        <Section 
            sectionId='portfolio'
            heading='portfolio'
        >
        </Section>
    );
};

export default Portfolio;